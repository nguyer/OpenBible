<UserControl
    x:Class="OpenBible.Views.SectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenBible.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="using:OpenBible.ViewModels"
	xmlns:vw="using:OpenBible.Views"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
	d:DataContext="{d:DesignInstance Type=vm:SectionViewModel, IsDesignTimeCreatable=True}">

	<StackPanel>
		<TextBlock Text="{Binding Title}" Style="{StaticResource SubtitleTextBlockStyle}"/>
		<ItemsControl x:Name="paragraphs" VerticalAlignment="Top" HorizontalAlignment="Stretch" ItemsSource="{Binding Paragraphs}" ScrollViewer.VerticalScrollBarVisibility="Disabled">
			<ItemsControl.ItemTemplate>
				<DataTemplate>
					<vw:ParagraphView/>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</StackPanel>
</UserControl>
