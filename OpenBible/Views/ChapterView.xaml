<UserControl
    x:Class="OpenBible.Views.ChapterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OpenBible.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:vm="using:OpenBible.ViewModels"
	xmlns:vw="using:OpenBible.Views"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
	d:DataContext="{d:DesignInstance Type=vm:ChapterViewModel, IsDesignTimeCreatable=True}">

	<ScrollViewer VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
		<StackPanel HorizontalAlignment="Stretch" Padding="0,25">
			<TextBlock x:Name="title" Style="{StaticResource TitleTextBlockStyle}">
				<Run Text="{Binding BookName}"/>
				<Run Text="{Binding Number}"/>
			</TextBlock>
			<ListView x:Name="sections" VerticalAlignment="Top" HorizontalAlignment="Stretch" ItemsSource="{Binding Sections}" SelectionMode="None">
				<ListView.ItemTemplate>
					<DataTemplate>
						<vw:SectionView/>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackPanel>
	</ScrollViewer>
</UserControl>
